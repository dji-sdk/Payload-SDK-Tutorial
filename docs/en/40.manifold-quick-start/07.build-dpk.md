## Application Packaging Process
Packaging a DPK application involves using `build_dpk.sh` DJI packaging tool to convert a development-state Manifold application package into a release-state installation package(xxx.dpk file). After generating the xxx.dpk file, it can be published for users to install and run. The overall process is as follows:

<div align=center>
<img src="https://terra-1-g.djicdn.com/71a7d383e71a4fb8887a310eb746b47f/psdk/app_mgr/11.png" width=60% style="height:auto"/>
</div>

<br>For development-state application packages, they must contain the files listed in **Table 1: Development-State Application Package Files** (source code files, app.json, and CMakeLists.txt are mandatory, configuration files, resource files, and third-party library dependencies may not be required depending on the actual situation):

**Table 1** Development-State Application Package Files
| File Type | Description | Mandatory |
|-----------|------------|-----------|
| Source Code Files | Application source files (.h, .c, or .cpp) | Yes |
| Resource Files | Application resource files (currently not required, but may include icon files in the future) | No |
| Configuration Files | Dependency files needed during runtime, with paths specified in the `userconfig` field of app.json | No |
| Third-Party Library Files | Must be static libraries, dynamic libraries not supported | No |
| app.json Configuration File | Declares app name, version, description, etc. | Yes |
| CMakeLists.txt Build File | Compilation configuration file for packaging | Yes |

The release-state application package is the final Manifold application installation package(xxx.dpk file), containing executable files and configuration files.

## Packaging Tool Description
`build_dpk.sh` is used to generate xxx.dpk file. It must be called in the CmakeLists.txt file after compiling the executable. The tool is located at `tools/build_dpk/build_dpk.sh`. Usage:

```bash
# Usage
build_dpk.sh -i input app.json file
    [-o output dpk file path]
    [-h help]
Options:
   -h        Show help
   -i        Specify input app.json
   -o        Specify output .dpk path
```

## <a name="app-json-configuration">app.json Configuration File</a>
The app.json is a very important file that must be included in the application code. Without it, the xxx.dpk cannot be generated.

### app.json Sample

Below is an app.json example (`samples/sample_c/platform/linux/manifold3/app_json/app.json`):
```json
  {
      "user_app_id": "226",
      "firmware_version": "01.00.00.00",
      "is_ai_rendering": "false",
      "platform": "manifold3",
      "name":{
        "name_cn": "测试demo",
        "name_en": "psdk-demo",
        "name_jp": "テストデモ",
        "name_fr": "Démo de test"
      },
      "description": {
        "description_cn": [
            "这是第一行",
            "这是第二行",
            "这是第三行",
            "这是第四行"
        ],
        "description_en": [
            "This is the first line",
            "This is the second line",
            "This is the third line"
        ],
        "description_jp": [
            "これは一行目です",
            "これは二行目です"
        ],
        "description_fr": [
            "Ceci est la première ligne"
        ]
      },
      "maintainer": {
        "maintainer_cn": "大疆创新科技有限公司",
        "maintainer_en": "DJI Innovation Technology Co., Ltd.",
        "maintainer_jp": "大疆イノベーションテクノロジー有限会社",
        "maintainer_fr": "Société à Responsabilité Limitée DJI Innovation Technology"
      },
      "ver_min": "00.00.00.00",
      "ver_max": "99.99.99.99",
      "bin": "../../../../../../build/bin/dji_sdk_demo_on_manifold3",
      "userconfig": [
        "../../../../../../samples"
      ]
  }
```

### app.json Description
The app.json file contains many fields. Below are the descriptions and specifications for each field:

| Field Name | Meaning | Data Type | Optional |
|-----------|---------|-----------|----------|
| user_app_id | The APP ID generated in the Developer Website User Center. It is the unique identifier of the application and is used to identify the uniqueness of the application. It **must be consistent with the generated APP ID**, that is, it must be consistent with `USER_APP_ID` in the code. The value will be verified during application installation. If it is inconsistent, the application installation will fail. | String | Required |
| firmware_version | The application version number, which can be used for application information display, that is, the application version displayed to users. <br>It consists only of numbers and dots, and adopts the four-segment format `xx.xx.xx.xx`. When the application is updated, the version number must be updated and **must be consistent with `firmwareVersion` in the code**, otherwise the application cannot be installed properly. <br>The meaning of the four segments is as follows: <ul><li>Segment 1: Major version / Major, range 0–99, used for major revisions such as new major features or significant changes.</li><li>Segment 2: Minor version / Minor, range 0–99, used for prominent feature additions or major issue fixes.</li><li>Segment 3: Debug version / Debug, range 0–99.</li><li>Segment 4: Modify version / Modify, range 0–99, used for maintenance versions such as bug fixes.</li></ul> | String | Required |
| is_ai_rendering | Indicates whether the application performs AI rendering on DJI Pilot. <br>If yes, set to `true`; if not, set to `false`. If not specified, the default value is `true`. | String | Optional|
| platform | Indicates the platform on which the application runs. Supported values are: <ul><li>`manifold3`: indicates that the application runs on a Manifold 3 device.</li></ul> | String | Required |
| name_cn | Indicates the Chinese name of the application. It can be customized and must be a string with a length not exceeding 47 bytes. <br>It is recommended that the application name be related to its functionality and avoid duplication with other application names. <br>This field can be used for application information display. For example, on the application management interface, when the PILOT language is set to Chinese, this field is used to display the application name. | String | Required |
| name_en | Indicates the English name of the application. It can be customized and must follow the naming rules below: <ul><li>Maximum length is 32 bytes.</li><li>Must not be `"null"`.</li><li>Only letters, numbers, hyphens (`-`), and dots (`.`) are allowed. No other characters are permitted.</li><li>Must start with a letter or a number.</li></ul> It is recommended that the application name be related to its functionality and avoid duplication with other application names. It is also recommended not to modify the application name after release. <br>This field can be used for application information display. For example, on the application management interface, when the PILOT language is set to English or other languages except Chinese, Japanese, and French, this field is used to display the application name. | String | Required |
| name_jp | Indicates the Japanese name of the application. It must be a string with a length not exceeding 47 bytes. <br>It is recommended that the name be related to the application functionality and avoid duplication with other application names. <br>This field can be used for application information display. For example, on the application management interface, when the PILOT language is set to Japanese, this field is used to display the application name. | String | Required |
| name_fr | Indicates the French name of the application. It must be a string with a length not exceeding 47 bytes. <br>It is recommended that the name be related to the application functionality and avoid duplication with other application names. <br>This field can be used for application information display. For example, on the application management interface, when the PILOT language is set to French, this field is used to display the application name. | String | Required |
| description_cn                                 | Identifies the application's Chinese description information. The value is a string not exceeding 367 bytes in length, containing a custom detailed description of the application. The specific filling rules are: <ul><li>Each item within [] represents the content of one line. The next item corresponds to the content of the next line. If a line break is required, multiple items can be entered within []. </li><li>eg. ["这是第一行", "这是第二行", "这是第三行"，"这是第四行"]，representing the application description：<br> "这是第一行<br>这是第二行<br>这是第三行<br>这是第四行"</li></ul>This field is used for displaying application information. For example, in the application management interface, when the PILOT language is set to Chinese, this field is used to display the application description. | String    | Required |
| description_en                                 | Identifies the application's English description. The value is a string not exceeding 367 bytes in length, containing a custom detailed description of the application. Cannot be “null”. Specific filling rules: <ul><li>Each item within [] represents the content of one line. The next item corresponds to the next line. To create line breaks, simply include multiple items within the []. </li><li>e.g., [“This is the first line”, “This is the second line”, “This is the third line”], representing the application description:<br> “This is the first line<br>This is the second line<br>This is the third line”</li></ul> This field is used for displaying application information. For example, in the application management interface, when the PILOT language is set to English or any language other than Chinese, Japanese, or French, this field is used to display the application description. | String    | Required |
| description_jp                                 | Identifies the Japanese description for the application. The value is a string no longer than 367 bytes, containing a custom detailed description of the application. The specific filling rules are: <ul><li>Each item within [] represents the content of one line. The next item represents the content of the next line. If line breaks are needed, multiple items can be filled within []. </li><li>eg. [“たそねめふふひのめ”, “ゅはのぬすひゃめぬぬぬ”] represents the following application description:<br> “たそねめふふひのめ<br>ゅはのぬすゃめぬぬぬ”</li></ul> This field is used for displaying application information. For example, in the application management interface, when the PILOT language is set to Japanese, this field is used to display the application description. | String    | Required |
| description_fr                                 | Identifies the French description for the application. The value is a string no longer than 367 bytes, containing a custom detailed description of the application. The specific filling rules are:<ul><li>Each item within [] represents the content of one line. The next item represents the content of the next line. If line breaks are needed, multiple items can be filled within []. </li><li>eg. [“Ceci est la première ligne”] represents the application description: <br>“Ceci est la première ligne”</li></ul> This field is used for displaying application information, such as in the application management interface when the PILOT language is set to French. | String    | Required |
| maintainer_cn | Indicates the developer information of the application in Chinese. It must be a string with a length not exceeding 127 bytes. <br>This field can be used for application information display. For example, on the application management interface, when the PILOT language is set to Chinese, this field is used to display the developer information. | String | Required |
| maintainer_en | Indicates the developer information of the application in English. It must be a string with a length not exceeding 127 bytes and must not be `"null"`. <br>This field can be used for application information display. For example, on the application management interface, when the PILOT language is set to English or other languages except Chinese, Japanese, and French, this field is used to display the developer information. | String | Required |
| maintainer_jp | Indicates the developer information of the application in Japanese. It must be a string with a length not exceeding 127 bytes. <br>This field is used for application information display. On the application management interface, when the PILOT language is set to Japanese, this field is used to display the developer information. | String | Required |
| maintainer_fr | Indicates the developer information of the application in French. It must be a string with a length not exceeding 127 bytes. <br>This field can be used for application information display. For example, on the application management interface, when the PILOT language is set to French, this field is used to display the developer information. | String | Required |
| ver_min | Indicates the minimum required Manifold 3 firmware package version for the application to run normally. When the Manifold 3 firmware package version is lower than this value, the application cannot be installed properly. <br>The value adopts the `xx.xx.xx.xx` four-segment format, and each segment ranges from 0 to 99. | String | Required |
| ver_max | Indicates the maximum required Manifold 3 firmware package version for the application to run normally. When the Manifold 3 firmware package version is higher than this value, the application cannot be installed properly. <br>The value adopts the `xx.xx.xx.xx` four-segment format, and each segment ranges from 0 to 99. | String | Required |
| bin | The relative path of the compiled application executable binary file, used to identify the application executable binary file. <br>The path must be a relative path, specifically relative to the `app.json` file. The corresponding binary file must exist under the specified path. | String | Required |
| userconfig | The relative path of configuration files, that is, files required by the application at runtime that are not integrated into the binary file. <br>The path must be a relative path, specifically relative to the `app.json` file. The corresponding file must exist under the specified path. If there are no related files, this field can be omitted. | String | Optional |

## Compilation and Packaging Steps
### 1. Modify Application Code
Update these files with your app info:

* samples/sample_c/platform/linux/manifold3/application/dji_sdk_app_info.h
* samples/sample_c++/platform/linux/manifold3/application/dji_sdk_app_info.h
```c
#define USER_APP_NAME               "your_app_name"
#define USER_APP_ID                 "your_app_id"
#define USER_APP_KEY                "your_app_key"
#define USER_APP_LICENSE            "your_app_license"
#define USER_DEVELOPER_ACCOUNT      "your_developer_account"
#define USER_BAUD_RATE              "460800"
```

And set firmware version:

* samples/sample_c/platform/linux/manifold3/application/main.c
```c
int main(int argc, char **argv)
{
    T_DjiReturnCode returnCode;
    T_DjiUserInfo userInfo;
    T_DjiAircraftInfoBaseInfo aircraftInfoBaseInfo;
    T_DjiAircraftVersion aircraftInfoVersion;
    T_DjiFirmwareVersion firmwareVersion = {
        .majorVersion = 1,
        .minorVersion = 0,
        .modifyVersion = 0,
        .debugVersion = 0,
    };
    ...
}
```

* samples/sample_c++/platform/linux/manifold3/application/application.cpp
```c++
void Application::DjiUser_ApplicationStart()
{
    T_DjiUserInfo userInfo;
    T_DjiReturnCode returnCode;
    T_DjiAircraftInfoBaseInfo aircraftInfoBaseInfo;
    T_DjiFirmwareVersion firmwareVersion = {
        .majorVersion = 1,
        .minorVersion = 0,
        .modifyVersion = 0,
        .debugVersion = 1,
    };
    ...
}
```
### 2. Create app.json
Refer to the app.json sample file and app.json description to create app.json. For details, see the chapter <a href = "#app-json-configuration">app.json Configuration File</a>.

### 3. Update CmakeLists.txt
First modify the top-level CMakeLists.txt file, changing:

```cmake
add_subdirectory(samples/sample_c/platform/linux/manifold2)
add_subdirectory(samples/sample_c++/platform/linux/manifold2)
```

to:

```cmake
add_subdirectory(samples/sample_c/platform/linux/manifold3)
add_subdirectory(samples/sample_c++/platform/linux/manifold3)
```


Next, refer to the build configuration file `samples/sample_c/platform/linux/manifold3/CMakeLists.txt` in the sample application.  
In this file, directly invoke the application packaging script `build_dpk.sh`, and provide the path to the `app.json` application-level configuration file as well as the output path for generating the `xxx.dpk` application installation package.  
After compilation, the `xxx.dpk` application installation package will be generated in the specified output path.
During application development, you can modify the path to the `app.json` application configuration file (`JSON_FILE`) and the output path for the `xxx.dpk` application installation package (`OUTPUT_PACKAGE`) in the `CMakeLists.txt` build configuration file according to your needs and the specific code structure. The final packaged `xxx.dpk` application installation package will be located in the `OUTPUT_PACKAGE` path you specified.

```cmake
# Set app.json and build_dpk.sh paths
set(JSON_FILE ${CMAKE_CURRENT_SOURCE_DIR}/app_json/app.json)
set(SCRIPT_FILE ${CMAKE_CURRENT_SOURCE_DIR}/../../../../../tools/build_dpk/build_dpk.sh)

# Set output dpk path
set(OUTPUT_PACKAGE ${CMAKE_BINARY_DIR}/dpk)

# Generate .dpk package
add_custom_command(TARGET ${PROJECT_NAME}
    POST_BUILD # Generate package after make
    COMMAND bash ${SCRIPT_FILE} -i ${JSON_FILE} -o ${OUTPUT_PACKAGE}
    DEPENDS ${JSON_FILE}
    COMMENT "Running build_dpk.sh to generate .dpk package from app.json"
)
```
### 4. Generate xxx.dpk
The PSDK supports CMake compilation, and xxx.dpk will be generated directly after compilation, use following commands:

```bash
cd Payload-SDK
mkdir build
cd build
cmake ..
make
```

After successful execution, the xxx.dpk package will be available at the `OUTPUT_PACKAGE` path, ready for installation and execution on Manifold devices.

## Application Installation Testing
After obtaining the DPK package, install/update it on the Manifold device. After successful installation, a directory will be created in /open_app partition, This directory contains all application files, and it will update automatically with app update.

To test the dpk app, you can start/stop the app via PILOT APP, and check whether the app runtime behavior meets expectations. if not, you can check application logs.

## FAQ
**1. How to fill in userconfig filed in app.json? How does the DPK app use packaged config files?**

Application packaging and runtime execution are decoupled processes.

Packaging Phase:
Application packaging organizes configuration files, executables, and other resources into a `xxx.dpk` installation package. The `userconfig` field in `app.json` specifies configuration file paths (relative to `app.json`) to include these files in the package. Paths may reference either: Entire directories, Specific files, Or a combination of both.

Runtime Phase:
After successful installation of the `.dpk` package, the Manifold3 device creates an application directory under `/open_app`. The application accesses files in this directory during execution.

Example Scenario:
In the app source code, the configuration files located at `/cfg/1/1.cfg`, `/cfg/2/2.cfg` , `/3/3.cfg`, and the app.json located at `/json/app.json`. Then valid `userconfig` formats and app runtime path include:

<table width="50%" style="display: table; table-layout:fixed; text-align:left">
  <thead>
    <tr>
      <th><code>userconfig</code> Format</th>
      <th><code>userconfig</code> Value</th>
      <th>Runtime Access Paths</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Directory-only</td>
      <td><code>["../cfg", "../3"]</code></td>
      <td>
        <code>"1/1.cfg"</code>, <code>"2/2.cfg"</code>, <code>"3.cfg"</code>
      </td>
    </tr>
    <tr>
      <td>File-specific</td>
      <td>
        <code>["../cfg/1/1.cfg", "../cfg/2/2.cfg", "../3/3.cfg"]</code>
      </td>
      <td>
        <code>"1.cfg"</code>, <code>"2.cfg"</code>, <code>"3.cfg"</code>
      </td>
    </tr>
    <tr>
      <td>Hybrid</td>
      <td><code>["../cfg", "../3/3.cfg"]</code></td>
      <td>
        <code>"1/1.cfg"</code>, <code>"2/2.cfg"</code>, <code>"3.cfg"</code>
      </td>
    </tr>
  </tbody>
</table>

**2. Installation fails with error: "Failed to start application service. Restart device and try again"**

**Solutions:**

- check app source code, to ensure all dependencies are static libraries (dynamic libraries unsupported) and the executable file can run normally.
- Check related logs at /blackbox/system/app_temp_files to investigate whether the application start normally and the reasons for any failure to start.
- If issues persist after fixing code, reboot device or contact DJI support.

**3. Installation fails with error: "Failed to verify application information. Make sure application information complies with specification"**

**Solutions:**

- Check whether the user_app_id and firmware_version fields in the app.json file comply with the specifications, refer to app.json description for details.
- If the values of the aforementioned fields are consistent, it is necessary to further check whether the application started normally without errors during installation. The startup logs during application installation can be found in the /blackbox/system/app_temp_files directory on the Manifold3 device.

**4. After the application is installed successfully, it fails to start, or although it starts successfully, the application's behavior does not match expectations:**

**Solutions:**
- Please ensure that all dependency files required by the application are packaged into the xxx.dpk file, and that the file read/write paths in the code are correct. For the correct read/write paths of dependency files, refer to the solution in: `Question 1. How to fill in userconfig filed in app.json? How does the DPK app use packaged config files?`
- If the application's dependency file read/write paths are correct and all dependencies are packaged into the xxx.dpk file, you can examine the application's runtime logs to debug reasons for startup failure or startup success with unexpected behavior.

**5.  The application depends on third-party libraries, and a permission issue is reported when installing the third-party library**

**Solutions:**

- Root accounts are not supported. Please include the dependency library in your application as a static library or source code.